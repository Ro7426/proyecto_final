<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 



<div class="container register-form">
            <div class="form">
                <div class="note">
                    <h3>INGRESA LOS DATOS DE TU LIBRO.<h3>
                </div>

                <div class="form-content">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                
                                <%= form.text_field :title, class:'form-control', placeholder: 'Titulo' %>
                            </div>
                            <div class="form-group">
                                <%= form.text_field :author, class:'form-control', placeholder: 'Autor' %>
                                
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <%= form.text_field :editorial, class:'form-control', placeholder: 'Editorial' %>
                            </div>
                            <div class="form-group">
                                <%= form.text_field :description, class:'form-control', placeholder: 'Descripcion' %>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <div class="field">
                                <%= form.label :photo %>
                                <%= form.file_field :photo %>
                                </div>
                            </div>
                           
                        </div>
                    </div>
                      <div class="actions">
                      <%= form.submit 'Ingresar', class: 'btnSubmit' %>
                      </div>
                    
                </div>
            </div>
        </div>
<% end %>
