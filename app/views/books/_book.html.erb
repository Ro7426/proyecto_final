


<div class="col-lg-3 col-sx-12">

  <div class="card-img-top img-fluid" style="width: 20rem;">
    <%= image_tag(book.photo, class: "card-img-top img-fluid", alt:"...", style:"width:50%") if book.photo.attached? %>
    <div class="card-body">
      
      <th>Dueño del Libro</th>
      <h6 class="card-title"><%= book.user.try(:name) %></h6>
      <th>Titulo</th>
      <h6 class="card-title"><%=book.title %></h6>
      <th>Autor</th>
      <h6 class="card-title"><%= book.author %></h6>
      <tr>
        <td></td>
        <td></td>


        <div class="row">
        <div class="col-lg-3 col-sx-12 text-right">
        <div class="btn-group d-flex w-100" role="group">

        
        <td><%= link_to 'VER', book, :class => 'btn btn-outline-primary' %></td>
         
         <% if user_signed_in? %>

         <% if current_user.id == book.user.id %>

          <td><%= link_to 'EDITAR', edit_book_path(book), :class => 'btn btn-outline-success' %></td>
          <td><%= link_to 'ELIMINAR', book, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-outline-danger ' %>
        
        <% end %>
        <% end %>

          </div>
          </div>
          </div>





        </td>
      </tr>  
    </div>
  </div>
</div>
 