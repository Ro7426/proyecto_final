 <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
  <!-- Brand -->
  <%# <a class="navbar-brand" href="#">Logo</a> %>
  <a class="navbar-brand" href="#">
    <%# <img src="assets/icono2.png" alt="log1" style="width:40px;"> %>
    <%= image_tag "icono2.png", alt: 'log1', style:'width:40px;' %>
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <!-- Links -->
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" href="<%=pages_home_path%>">INICIO</a>  
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%=books_path%>">LISTA DE LIBROS</a>
      </li>
      <li class="nav-item">

        <% if user_signed_in? %>
              <%= link_to "MIS LIBROS", books_path(current: current_user.id), class: "nav-link" %>
        <% end %>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%=new_book_path%>">INGRESAR UN LIBRO</a>
      </li>

      

      <!-- Dropdown -->
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbardrop" data-toggle="dropdown">
        <% if user_signed_in? %>
        <%= current_user.email %>
        <% else %>
          REGISTRATE
        <% end %>
        </a>
        <div class="dropdown-menu">
          <% if user_signed_in? %>

          

          <a class="dropdown-item" href="<%=destroy_user_session_path%>" data-method="DELETE">Salir</a>
          
          <% else %>
               
              <a class="dropdown-item" href="<%= new_user_session_path%>">Ingreso</a>
              <a class="dropdown-item" href="<%=new_user_registration_path%>">Registro</a>
              
          <% end %>
        </div>
      </li>
    </ul>
  </div>
</nav> 